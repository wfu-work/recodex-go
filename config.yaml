server:
  host: "127.0.0.1"
  port: 8765

codex:
  mode: "cli"
  binary: "codex"

state:
  dir: ".recodex"

security:
  pairing_enabled: true
  pairing_ttl_seconds: 300
  require_confirm_for_git_write: true
